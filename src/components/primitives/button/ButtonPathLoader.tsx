import type { FC } from 'react';

interface ButtonPathLoaderProps { }

const ButtonPathLoader: FC<ButtonPathLoaderProps> = ({ }) => {
  return (
    <div>
      ButtonPathLoader
    </div>
  );
}

export default ButtonPathLoader;