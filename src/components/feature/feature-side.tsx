import type { ComponentProps, FC } from 'react';

const FeatureSide: FC<ComponentProps<'div'>> = ({ className, ...rest }) => {
  return (
    <div>
      FeatureSide
    </div>
  );
};

export default FeatureSide;